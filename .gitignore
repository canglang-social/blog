# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# unfinished writing (decided 2026-08-10, TODO.md item 1)
# This repo is PUBLIC and editor.md defines it as a curated,
# hiring-manager-visible surface. drafts/ holds work in progress, so
# committing it would publish unfinished writing on exactly the surface
# built to show finished work. It was previously neither tracked nor
# ignored — an undefined state that looked handled and wasn't.
# Backup: the vault, not a new mechanism. Anything in here worth keeping
# gets pasted to [[Writing/Seeds]], which is already synced.
drafts/

# git worktrees created for cross-repo PRs
.git-worktrees/
